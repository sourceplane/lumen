{
  "name": "@saas/flows-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Contract tests for the bootstrap flows' shared machinery (saas-baseline-tracking BT6): track.sh over a fake orun, land-pr.sh over a fake pen and gh. bash + python3 + git, no network.",
  "scripts": {
    "build": "node -e \"process.exit(0)\"",
    "typecheck": "node -e \"process.exit(0)\"",
    "test": "bash ../../flows/testing/track.test.sh && bash ../../flows/testing/land-pr.test.sh"
  }
}
