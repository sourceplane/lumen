# webhook-verifier

Shared workspace package.

A shared package of the lumen workspace, consumed by the components
below at build time.

## Depended on by

- **cli** — First-class TypeScript CLI for the Lumen control plane API
